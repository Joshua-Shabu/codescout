{
  "name": "codescout-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Agentic AI backend that lets Claude explore a local codebase via tool-calling to answer questions about it. Zero runtime dependencies — calls the Anthropic Messages API directly over fetch.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch --env-file=.env src/index.ts",
    "build": "tsc",
    "start": "node --env-file=.env dist/index.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
